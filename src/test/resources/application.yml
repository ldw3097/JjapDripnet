spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/Codes/jjapdripnet_db/jjapdripnet
    username: ldw3097
    password: asdf1234
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.sql: debug

server:
  servlet:
    session:
      tracking-modes: cookie